server:
  port: 8090
spring:
  application:
    name: library-client
  cloud:
    config:
      fail-fast: true
      retry:
        max-attempts: 5
        initial-interval: 1000
        max-interval: 5000
        multiplier: 2
      uri: ${CONFIG_SERVER_URI:http://localhost:9000}

