server:
  port: 8090
spring:
  application:
    name: library-client
  cloud:
    config:
      fail-fast: true
      retry:
        max-attempts: 10
        initial-interval: 2000
        max-interval: 45000
        multiplier: 2

